name: "kimbles-rte-player CodeQL config"

# Analyse only first-party source. vendor/ holds ~425 MB of third-party
# binaries and bundled code; scanning it exhausts runner disk and produces
# alerts we do not own.
paths:
  - src
  - scripts

paths-ignore:
  - vendor
  - node_modules
  - build
  - tests
